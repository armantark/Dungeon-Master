# Papercuts

- 2026-08-19 13:22:13 PDT — While consolidating branches, `git merge -F -` failed with `error: could not read file '-'`; this Git invocation does not treat `-` as stdin for merge messages, so use `git merge -m` instead.
- 2026-08-19 13:56:58 PDT — While delegating the isometric frontend to K3, the max-effort client ran for about 24 minutes without writing a file and ended with `error: provider request failed after 3 attempts: The read operation timed out`; bounded high- and low-effort retries later returned the identical error without writing a file.
- 2026-08-19 14:33:00 PDT — While starting the atlas browser smoke server, port `127.0.0.1:8765` was already owned by an unrelated Python JSON service, so the static artifact server moved to port `8766`.
- 2026-08-19 14:38:56 PDT — While checking the atlas at a mobile viewport, the T3 preview loaded and resized the page but snapshot, evaluate, click, and recording calls timed out; desktop pixels and all interactions were verified through PinchTab instead.
- 2026-08-19 14:38:56 PDT — While publishing the interactive atlas, Postplan returned `Inline JavaScript requires an authenticated upload.` and `npx postplan whoami` returned `Missing or invalid API key.`; the canonical local artifact remains complete, but mobile hosting needs Postplan authentication.
- 2026-08-19 16:03:59 PDT — While pinning the atlas painter-order regression, the first source assertion guessed a stale `box(...)` call shape instead of inspecting the current `prism(...)` helper, so the check was tightened against the real tower and annex draw calls.
- 2026-08-19 16:38:54 PDT — While proving the architecture endpoint was development-only, the first production build still emitted `ArchitectureApp-*.js` because `import.meta.env.DEV` was passed through a helper; Vite removed the chunk only after the dynamic import moved inside a literal compile-time dev branch.
- 2026-08-19 16:38:54 PDT — On Node `v26.5.0`, plain `npm test` failed 27 existing jsdom storage tests with `ExperimentalWarning: localStorage is not available because --localstorage-file was not provided.` and `TypeError: Cannot read properties of undefined (reading 'clear')`; a temporary `NODE_OPTIONS=--localstorage-file=<path>` plus `--maxWorkers=1` made all 320 tests pass without changing product code.
- 2026-08-19 16:47:00 PDT — While requesting the bounded Opus diff review, `claude_prompt.sh --model opus --read-only-tools` produced zero output for ten minutes and had to be interrupted; no review verdict was claimed.
- 2026-08-19 22:58:34 PDT — While probing the running Vite endpoint in zsh, a loop variable named `path` overwrote zsh's tied `$path`/`$PATH` command search array, causing `command not found: curl` and `command not found: rg`; use a task-specific name such as `route_path`.
- 2026-08-20 15:29 PDT — Rebuilding the dev architecture map with Kimi K3: fresh quota reported 100% five-hour and 85% weekly remaining, but `https://api.kimi.com/coding/v1/messages` returned no response headers or file edits for more than three minutes and had to be interrupted inside `httpcore._sync.http11._receive_response_headers`.
- 2026-08-20 15:40 PDT — Inspecting Kimi's official streaming implementation: an unquoted `?recursive=1` in the `gh api` path triggered zsh's `no matches found`; quote GitHub API paths that contain query parameters.
- 2026-08-20 15:45 PDT — Disposing of the isolated K3 canary: the command runner rejected the exact temporary-directory `rm -rf` as unsafe even after the target was validated; move disposable probes to Trash instead.
- 2026-08-20 15:51 PDT — Rebuilding the architecture map through the repaired K3 stream: the unrestricted read/search transcript grew until Kimi returned `Invalid request: Your request exceeded model token limit: 1048576 (requested: 1317789)` before the first file write; delegated tool results need a hard context budget.
- 2026-08-20 16:00 PDT — Installing K3's architecture graph dependencies: npm completed, but reported 7 audit findings (2 moderate, 5 high) and blocked the optional `fsevents@2.3.3` install script because it is not covered by `allowScripts`.
- 2026-08-20 16:01 PDT — Interrupting the apparently idle K3 map rebuild: the process reported no writes before termination, but buffered edit calls landed files seconds later; always recheck `git status` after terminating a delegated editor before declaring that it made zero edits.
